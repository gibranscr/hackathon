Open anaconda
The file Wit-APPV3-final-backup.ipynb is used with anaconda so that you just need to upload this file to the folder where you store your anaconda files.
Do the pip install command to get the libraries that are indicated in the file requirements2.txt For example: pip install yagmail would be te command to install yagmail library in your anaconda
Run the file Wit-APPV3-final-backup.ipynb onces you installed all the libraries specified in the requirements2.txt file

The application should run and the email to get the results by this reception method can be verified with username:anunciosgibsa@gmail.com" and password:VentaProduct51g